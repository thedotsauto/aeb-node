# Mac-side viewer dependencies.
#
# Install into a virtual environment:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r viewer/requirements.txt
#
# Versions are lower-bounded rather than pinned: this is a development tool,
# not vehicle software, and nothing in the safety path depends on it.
matplotlib>=3.7
numpy>=1.24
